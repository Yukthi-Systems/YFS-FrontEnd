{
  "name": "yfs-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @yfs/web",
    "build": "npm run build -w @yfs/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
